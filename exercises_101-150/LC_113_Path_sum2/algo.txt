Create helper function that accespts treeNode, temp array and sum of current iteration. Id current treenode is null return. Otherwise push curret treeNode value to helper array. Check if elft and right nodes are null. if true check if intermediate sum and current value is equal to target, if yes - push current helper array to result. if not recurse to node left and right and increase intermediate sum by current value.